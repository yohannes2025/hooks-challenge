import React from "react";
import styles from "./Loader.module.css"; // Import the CSS file for styling

const Loader = () => {
  return <div className={styles.loader}></div>;
};

export default Loader;
